# 2026-05-01

- Re-ran the `medio-punto.com` connectivity probe. `http://medio-punto.com` still redirects to `http://www.medio-punto.com/` via Namecheap URL Forward and apex HTTPS still times out.
- `www.medio-punto.com` DNS still points at Namecheap parking (`parkingpage.namecheap.com` / `91.195.240.19`). Earlier probes returned Namecheap parking (`HTTP/2 405`, `server: Parking/1.0`); the latest HTTPS probe timed out instead, so the public route is still broken even if the exact edge response is wobbling.
- Local preview is healthy: a Node process is listening on `127.0.0.1:3101`, `127.0.0.1:3101` serves the current first-drop storefront content, and the in-cluster ingress route for `medio-punto.local` returns HTTP 200 with the same storefront.
- Kubernetes state is still only the local preview path: `service/medio-punto-web` remains an `ExternalName` to `host.docker.internal:3101`, the ingress host is still `medio-punto.local`, and the Argo-managed workload still includes only the placeholder `medio-punto-statics` deployment/service.
- The app repo at `github/rfcku/medio-punto` is now clean and has the storefront rewrite committed locally as `1fbef60` (`feat: reframe storefront as phase 1 drop preview`), but `main` is still ahead of `origin/main` by one commit, so the good storefront state is not pushed upstream yet.
- OpenClaw cron delivery failures were traced to missing WhatsApp targets, not job execution failures. Three cron jobs were updated in `/Users/rfcku/.openclaw/cron/jobs.json` to use explicit WhatsApp delivery with `channel: "whatsapp"`, `to: "+5216642800707"`, and `bestEffort: true`.
- Discord access was verified as working and then hardened in `/Users/rfcku/.openclaw/openclaw.json`: `channels.discord.groupPolicy` was changed to `allowlist`, guild `870150520789168188` (`sótano`) was allowlisted, `requireMention` was enabled, and the currently used channels were allowlisted.
- The active Obsidian vault is `/Users/rfcku/Documents/BishopVault`. A `Channels/` section was created with platform indexes plus per-entity notes for WhatsApp chats and the Discord guild/channels; root `INDEX.md` was updated to link it.
- MemPalace is installed and usable on this machine (`/Users/rfcku/.local/bin/mempalace`, version `3.3.4`, palace with 2018 drawers). There is also a local OpenClaw skill at `~/.openclaw/workspace/skills/mempalace/SKILL.md`. Current stance: useful as external memory/search, not a replacement for native `MEMORY.md`.
- `github/rfcku/ditto-cli` audited cleanly at the basic build level: `make test`, `make lint`, and `make build` all passed. Main product findings were functional inconsistencies rather than hard build failures.
- Highest-priority `ditto-cli` audit findings: CLI `vote post` uses the wrong target type (`0` instead of post type `2`), TUI “create community” path routes to community browsing instead of creation, search results can include posts but Enter only opens communities, share URLs are built from the API base URL, and the TUI/docs promise features like join/profile behavior that are still placeholders or incomplete.
- `ditto-cli` security and delivery gaps worth remembering: login/register/token flags expose secrets via shell history and process lists, default remote usage does not strongly discourage plain HTTP, CI does not run smoke tests or race tests, and local lint uses `golangci-lint@latest` while CI pins a specific version.
- The Argo CD GitOps repo for cluster changes is `/Users/rfcku/.openclaw/workspace/github/argocd` (`https://github.com/bshop341-b/argocd.git`). OpenClaw chart source was switched there to `https://serhanekicii.github.io/openclaw-helm`, chart `openclaw`, version `1.5.29`, in commit `6bddc81` (`feat: switch openclaw to serhanekicii helm chart`).
- The `serhanekicii/openclaw` chart is not a drop-in replacement for the previous chart. OpenClaw values now use the app-template style structure (`controllers.main.containers.*`, `configMaps.config.data.openclaw.json`, `persistence.data`, `ingress.main`, `envFrom.secretRef`).
- User wants OpenClaw standardized to one Argo CD project (`openclaw`) and one shared Kubernetes namespace (`openclaw`) for all agents, while keeping separate per-agent Argo app and Helm release names.
- The shared-namespace migration was not actually clean at the last confirmed check: Argo reported `OutOfSync` / `Missing` because the desired PVC storage request attempted to shrink below the existing PVC capacity (`spec.resources.requests.storage` cannot be less than `status.capacity`). At that point an older `openclaw-cleo` runtime was still the one observed running, with images `ghcr.io/openclaw/openclaw:2026.3.13-1` and `zenika/alpine-chrome:124`.
