# 2026-04-30

- Followed up on the `medio-punto.com` connectivity check. The public domain is still parked at Namecheap: apex resolves to `192.64.119.180`, `www` resolves to Namecheap parking, `http://medio-punto.com` redirects to `http://www.medio-punto.com/`, `https://medio-punto.com` times out, and `https://www.medio-punto.com` returns `Parking/1.0` 405.
- Confirmed the Argo CD app `medio-punto` is `Synced` and `Healthy`, but the deployed workload is only a placeholder `nginx:stable-alpine` `Deployment` plus a `ClusterIP` `Service`; there is no public production ingress in the app resources.
- The only exposed ingress for Medio Punto in-cluster is the local preview route `medio-punto.local`, which forwards to `host.docker.internal:3101` via the `workloads/local-sites` manifests.
- Fetched the `statics` Helm chart revision used by Argo (`48da8ea9cb75c58693772c3bdee25c51e03c096b`) and confirmed it is a minimal generic nginx chart. Its ingress template currently has an API version typo (`networking.k8s.io/1`), though that path is not active because `ingress.enabled` is false for the deployed app.
- Net result: Medio Punto is blocked on both public DNS/registrar changes and missing GitOps production configuration, not just DNS alone.
