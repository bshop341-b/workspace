# 2026-04-27

- User shared contact info for mytzuko:
  - WhatsApp: +52 664 123 5340
  - Instagram: https://www.instagram.com/mytzuko.a.nidome?igsh=MWI4dHF4c2J0bzdnZQ==
- User added the authenticated GitHub account `bshop341-b` as a collaborator on the relevant repos.
- Accepted pending GitHub repository invitations for `rfcku/ditto` and `rfcku/ditto-cli`.
- Added pending tasks to stand up local cluster hostnames for `mytzuko.local`, `stitching-octopus.local`, and `nctto.local`, following the `ditto.local` pattern.
- User decided not to use MusicBrainz for now; moved it to backlog as a later analysis item for Metal API.
- User decided to disconnect Mytzuko, Stitching Octopus, and nctto from Vercel and keep them local-only under `.local`; updated pending tasks accordingly.
- Enabled explicit listening in WhatsApp group `120363429037120942@g.us` (373) by setting `requireMention: false` for that group.
- Found and fixed WhatsApp group routing config: `groupAllowFrom` had been holding group JIDs, but OpenClaw treats it as sender allowlist inside groups. Migrated known group JIDs into `channels.whatsapp.groups`, set 373 to `requireMention: false`, left the other allowlisted groups mention-gated, and set `groupAllowFrom` to `[*]` so messages can actually arrive inside those allowed groups.
- User clarified that the nctto repo is named `lumenforge` in the `bshop` GitHub account.
