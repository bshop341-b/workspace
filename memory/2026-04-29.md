# 2026-04-29

- A recurring `lumenforge` local preview failure was caused by `MONGODB_URI=mongodb://localhost:27017/test` with no listener on port `27017`.
- Started a local Docker MongoDB container named `openclaw-local-mongo` on `127.0.0.1:27017`, verified `mongosh` ping returned `{ ok: 1 }`, and local preview ports `3104` and `3116` returned HTTP 200 again.
