# 2026-04-25

- User set new communication preferences: keep replies short and summarized by default, speak casually like a human, avoid always ending with next-step suggestions, and use emojis/GIFs/images only when they fit naturally.
# 2026-04-25

- User set new communication preferences: keep replies short and summarized by default, speak casually like a human, avoid always ending with next-step suggestions, and use emojis/GIFs/images only when they fit naturally.
- Canonical local GitHub mirror path is `~/.openclaw/workspace/github`, not `~/.openclaw/github`.
- Updated the Obsidian vault to reflect the new GitHub mirror path, with GitHub notes acting as technical project sheets.
- Ensured all Obsidian directories have `INDEX.md` and `TASKS.md` (ignoring `.obsidian`).
- Added GitHub-backed projects to the Obsidian Projects section: Mytzuko, Borders, Metal API, Ditto, Stitching Octopus, and updated Medio Punto to the correct mirror path.
- `ditto-apis` in Argo CD was repaired and is now `Synced + Healthy`; added a simple GitOps-managed `ditto-mongodb` app and kept app secrets manual because there is still no GitOps-safe secret management stack installed.
# 2026-04-25

- User set new communication preferences: keep replies short and summarized by default, speak casually like a human, avoid always ending with next-step suggestions, and use emojis/GIFs/images only when they fit naturally.
# 2026-04-25

- User set new communication preferences: keep replies short and summarized by default, speak casually like a human, avoid always ending with next-step suggestions, and use emojis/GIFs/images only when they fit naturally.
- Canonical local GitHub mirror path is `~/.openclaw/workspace/github`, not `~/.openclaw/github`.
- Updated the Obsidian vault to reflect the new GitHub mirror path, with GitHub notes acting as technical project sheets.
- Ensured all Obsidian directories have `INDEX.md` and `TASKS.md` (ignoring `.obsidian`).
- Added GitHub-backed projects to the Obsidian Projects section: Mytzuko, Borders, Metal API, Ditto, Stitching Octopus, and updated Medio Punto to the correct mirror path.
- `ditto-apis` in Argo CD was repaired and is now `Synced + Healthy`; added a simple GitOps-managed `ditto-mongodb` app and kept app secrets manual because there is still no GitOps-safe secret management stack installed.
- Ditto now uses a dedicated GitOps layout in the Argo repo: Ditto apps live under `applications/ditto/`, Ditto platform resources under `workloads/ditto-platform/`, and Helm overrides under `values/ditto/`.
- The Ditto API is exposed through ingress-nginx at `https://api.ditto.local` with cert-manager TLS, and local host mapping may still need to be added manually on the machine.
- After an upstream Ditto Helm chart update added a UI subchart, a new `ditto-ui` Argo app was added for `ditto.local`, with values in `values/ditto/ui/values.yaml` and a TLS cert for `ditto.local`.
- `ditto-ui` is currently blocked by image architecture mismatch: `ghcr.io/rfcku/vora-web:main` exists but only publishes `linux/amd64`, while the local kind cluster nodes are `linux/arm64`, causing `ErrImagePull` / `no match for platform in manifest`.
- The same architecture issue was confirmed for the `vora-web` tags `main`, `latest`, and `sha-f3d7a38`; the UI image needs a multi-arch publish including at least `linux/arm64`.
