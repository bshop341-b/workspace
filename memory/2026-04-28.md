# 2026-04-28

- Applied `github/argocd/workloads/local-sites` for the local preview routes.
- Confirmed `https://nctto.local`, `https://mytzuko.local`, and `https://stitching-octopus.local` all return HTTP 200 through the ingress controller when resolved to `172.18.255.200`.
- `nctto.local` currently serves from the `bshop341-b/lumenforge` preview on port `3104`.
- Fixed the local Auth.js host-trust issue in `github/bshop341-b/lumenforge` by setting `trustHost: true` in `src/auth.ts` and documenting `AUTH_URL` / `AUTH_TRUST_HOST` in the repo docs.
- MongoDB-backed auth and DB flows in `lumenforge` still require a reachable Mongo instance; local starts against `mongodb://localhost:27017/test` continue to fail when nothing is listening on port `27017`.
- Diagnosed `medio-punto.com` routing: the local preview on port `3101` serves the updated Next.js app, but the public domain is still on Namecheap parking. DNS currently resolves apex to `192.64.119.180` and `www` to `parkingpage.namecheap.com` / `91.195.240.19`; `http://medio-punto.com` 302s to `http://www.medio-punto.com/`, `https://medio-punto.com` times out, and `https://www.medio-punto.com` returns `Parking/1.0` 405.
- User clarified a standing rule: all apps they manage must be deployed through GitOps and Argo CD, always. Manual cluster-only applies do not count as done.
