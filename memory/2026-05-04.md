# 2026-05-04

- Followed up on the latest `medio-punto` async checks. `npm run build` passed again in `github/rfcku/medio-punto`, and `PORT=3101 npm run start` restored the local preview so `http://127.0.0.1:3101/` returned `HTTP 200` again.
- Re-smoke-tested the local ingress path with `curl -k --resolve medio-punto.local:443:127.0.0.1 https://medio-punto.local/` and confirmed it now returns `HTTP/2 200`, so the `.local` route is healthy when the host-run preview is alive.
- Public `medio-punto.com` is still not serving the app. Apex still forwards through Namecheap and apex HTTPS still times out, while `www.medio-punto.com` is still on Namecheap parking instead of the Next.js storefront.
- The storefront repo state is no longer the blocker. `github/rfcku/medio-punto` is clean on `main`, and `origin/main` is already at `1fbef60` (`feat: reframe storefront as phase 1 drop preview`).
- The current GitOps shape is still local-preview-only for Medio Punto: `applications/statics/medio-punto.yaml` points at the placeholder `charts/statics` app with empty values, while the only concrete working ingress in the repo is `workloads/local-sites/medio-punto`, which targets `medio-punto.local` -> `host.docker.internal:3101`.
- There is still no production delivery path checked in for `github/rfcku/medio-punto`: unlike sibling Next.js sites, the repo has no `Dockerfile` or deployment workflow, so there is no image build/publish path for Argo CD to consume yet.
- The fallback `charts/statics` Helm chart is not production-ready for a public ingress switch either. Its ingress template still uses the invalid API version `networking.k8s.io/1`, so enabling ingress there would need a chart fix before Argo CD could apply it cleanly.
- Ran a fresh `github/rfcku/ditto-cli` audit from a temp clone on `main`: `go test ./...`, `make lint`, and `make build` all passed, and the TUI launched cleanly against `https://api.ditto.local/v1` and loaded the authenticated feed.
- Live CLI runtime pass confirmed `register`, `get me`, `create community`, `create post --community <name>`, `get feed`, `describe post`, `get posts --random`, `get comments --random`, `get users --random`, and `vote post` all worked against the real Ditto API.
- Real regressions still visible in runtime: `join <community-id>` returns `404 page not found` both through the CLI and direct `POST /communities/:id/join` calls to `https://api.ditto.local/v1`, so join is currently backend-broken from the user perspective; media upload still fails server-side with `mkdir ./.tmp: read-only file system` and current `main` only shows a generic `upload failed: 500`.
- The current open `ditto-cli` issue state is down to `#158`, and PR `#163` is the matching fix that correctly surfaces backend error details like `upload failed (500): Failed to create file: mkdir ./.tmp: read-only file system` during live testing.
- Later that morning, the live `ditto-apis` Argo CD application was confirmed to source values from `https://github.com/bshop341-b/argocd.git` (`values/ditto/apis/values.yaml`), not `rfcku/argocd`. The correct GitOps update pinned the API image to commit `2fccfd4443954ee2772423e317cc2569d407fe13`, Argo auto-synced, and the in-cluster deployments rolled from `ghcr.io/rfcku/ditto:main` to the pinned SHA while returning to `Synced + Healthy`. A mistaken pin pushed to `rfcku/argocd` was immediately reverted.
- A follow-up Ditto UI heartbeat pass on May 4 showed the home feed rendering fresh smoke-test posts correctly at `https://ditto.local/`, but direct UI routes `/popular`, `/trending`, `/top`, and `/feed` still returned 404, and the community page still hit a cross-origin preflight failure on `GET /v1/subs/check/:id?type=1`, so Join/subscription state remains broken in the browser path even though community posts render.
- `github/argocd/scripts/ensure-local-previews.sh` completed cleanly on May 4. `medio-punto` and `nctto` were already listening on `3101` and `3104`, the script restarted `mytzuko` on `3102` and `stitching-octopus` on `3103`, and follow-up `curl -I` checks confirmed `HTTP 200` / `HTTP/2 200` for all four direct ports and all four `.local` ingress routes.
# 2026-05-04

- Followed up on the latest `medio-punto` async checks. `npm run build` passed again in `github/rfcku/medio-punto`, and `PORT=3101 npm run start` restored the local preview so `http://127.0.0.1:3101/` returned `HTTP 200` again.
- Re-smoke-tested the local ingress path with `curl -k --resolve medio-punto.local:443:127.0.0.1 https://medio-punto.local/` and confirmed it now returns `HTTP/2 200`, so the `.local` route is healthy when the host-run preview is alive.
- Public `medio-punto.com` is still not serving the app. Apex still forwards through Namecheap and apex HTTPS still times out, while `www.medio-punto.com` is still on Namecheap parking instead of the Next.js storefront.
- The storefront repo state is no longer the blocker. `github/rfcku/medio-punto` is clean on `main`, and `origin/main` is already at `1fbef60` (`feat: reframe storefront as phase 1 drop preview`).
- The current GitOps shape is still local-preview-only for Medio Punto: `applications/statics/medio-punto.yaml` points at the placeholder `charts/statics` app with empty values, while the only concrete working ingress in the repo is `workloads/local-sites/medio-punto`, which targets `medio-punto.local` -> `host.docker.internal:3101`.
- There is still no production delivery path checked in for `github/rfcku/medio-punto`: unlike sibling Next.js sites, the repo has no `Dockerfile` or deployment workflow, so there is no image build/publish path for Argo CD to consume yet.
- The fallback `charts/statics` Helm chart is not production-ready for a public ingress switch either. Its ingress template still uses the invalid API version `networking.k8s.io/1`, so enabling ingress there would need a chart fix before Argo CD could apply it cleanly.
- Ran a fresh `github/rfcku/ditto-cli` audit from a temp clone on `main`: `go test ./...`, `make lint`, and `make build` all passed, and the TUI launched cleanly against `https://api.ditto.local/v1` and loaded the authenticated feed.
- Live CLI runtime pass confirmed `register`, `get me`, `create community`, `create post --community <name>`, `get feed`, `describe post`, `get posts --random`, `get comments --random`, `get users --random`, and `vote post` all worked against the real Ditto API.
- Real regressions still visible in runtime: `join <community-id>` returns `404 page not found` both through the CLI and direct `POST /communities/:id/join` calls to `https://api.ditto.local/v1`, so join is currently backend-broken from the user perspective; media upload still fails server-side with `mkdir ./.tmp: read-only file system` and current `main` only shows a generic `upload failed: 500`.
- The current open `ditto-cli` issue state is down to `#158`, and PR `#163` is the matching fix that correctly surfaces backend error details like `upload failed (500): Failed to create file: mkdir ./.tmp: read-only file system` during live testing.
- Later that morning, the live `ditto-apis` Argo CD application was confirmed to source values from `https://github.com/bshop341-b/argocd.git` (`values/ditto/apis/values.yaml`), not `rfcku/argocd`. The correct GitOps update pinned the API image to commit `2fccfd4443954ee2772423e317cc2569d407fe13`, Argo auto-synced, and the in-cluster deployments rolled from `ghcr.io/rfcku/ditto:main` to the pinned SHA while returning to `Synced + Healthy`. A mistaken pin pushed to `rfcku/argocd` was immediately reverted.
- A follow-up Ditto UI heartbeat pass on May 4 showed the home feed rendering fresh smoke-test posts correctly at `https://ditto.local/`, but direct UI routes `/popular`, `/trending`, `/top`, and `/feed` still returned 404, and the community page still hit a cross-origin preflight failure on `GET /v1/subs/check/:id?type=1`, so Join/subscription state remains broken in the browser path even though community posts render.
- `github/argocd/scripts/ensure-local-previews.sh` completed cleanly on May 4. `medio-punto` and `nctto` were already listening on `3101` and `3104`, the script restarted `mytzuko` on `3102` and `stitching-octopus` on `3103`, and follow-up `curl -I` checks confirmed `HTTP 200` / `HTTP/2 200` for all four direct ports and all four `.local` ingress routes.
- Ditto demo hardening finished with backend fixes merged and live: `rfcku/ditto#45` restored the join route, `rfcku/ditto#46` fixed uploads on read-only deployments, and the live API was ultimately pinned to `ghcr.io/rfcku/ditto:543f7b0244567934b0e8510ed3cab4762b4e8b72`, which was validated healthy in Argo CD.
- Final live Ditto validation on May 4 succeeded for community join, post vote, and `.png` media upload against the deployed API, and uploaded media correctly surfaced as `mediaCount: 1` on the target post.
- A deeper fresh-local `main` audit in `/tmp/ditto-demo-warroom/ditto-cli` found two real client/TUI bugs: the API returns `subed` instead of `joined`, which broke community join state in the CLI/TUI, and the profile settings save flow could leave the UI in a bad state after updating the user.
- Local fixes were added in `ditto-cli` for both issues: `internal/types/types.go` now accepts either `joined` or `subed` for communities, `internal/api/client.go` accepts either `status` or `subed` from community status checks, and the settings update flow now refetches `GetMe()` and returns to `StateProfileSettings` with a success message.
- Focused regression coverage was added locally for those Ditto CLI fixes in `internal/types/community_test.go`, `internal/api/client_status_test.go`, and `internal/ui/settings_update_test.go`, and the targeted suites `go test ./internal/types ./internal/api ./internal/ui` passed after the fixes.
- The all-in-one Bubble Tea live harness approach proved too brittle because command draining could recurse indefinitely under internal Bubble Tea command loops, so the reliable audit strategy became smaller focused tests plus manual PTY validation against the real API.
- The deeper Ditto TUI audit eventually passed locally for the major interactive flows: feed, help, logs, search, community view, create post, join, upvote, comment, reply, author profile, share, and report all worked against the live API after the local client/TUI fixes.
- Important demo caveat: Ditto does not currently expose native media upload inside the TUI. Media upload exists in `cmd/upload.go` and works via CLI/API, while the TUI can render and reflect media-backed posts but cannot itself initiate an upload today.
