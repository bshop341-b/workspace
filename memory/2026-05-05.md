- Re-checked the latest async `medio-punto` probe on May 5. `npm run build` passed in `github/rfcku/medio-punto`, the host-run preview on `127.0.0.1:3101` is healthy again with `HTTP 200`, and `https://medio-punto.local/` also returns `HTTP/2 200` when resolved to `127.0.0.1` while `PORT=3101 npm run start` stays up.
- Public rollout is still blocked outside the repo: `https://medio-punto.com` continues to time out, and `https://www.medio-punto.com` is still serving Namecheap parking (`HTTP/2 405`, `server: Parking/1.0`) instead of the storefront.
- This confirms the same split as before: app code/local preview are fine, but DNS/registrar plus a real production delivery path are still missing for the public domain.
- A later async follow-up probe still hit Namecheap on the public route, surfacing `X-Served-By: Namecheap URL Forward Server`, `server: namecheap-nginx`, and an `HTTP/1.1 436` on the forwarded public path, so the registrar-side forwarding/parking issue remains unresolved.
