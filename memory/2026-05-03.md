# 2026-05-03

- Revalidated `medio-punto` after a real local-preview regression. `127.0.0.1:3101` had stopped listening and `medio-punto.local` was returning `502`, but `npm run build` still passed in `github/rfcku/medio-punto`, `PORT=3101 npm run start` restored the preview, and both `http://127.0.0.1:3101/` and `https://medio-punto.local/` (via `--resolve` to `172.18.255.200`) returned `200` again with the honest phase-1 storefront (`Tote tejido hero`, `Assisted order`, `Roadmap del drop`).
- Public `medio-punto.com` remains blocked at Namecheap. Apex still resolves to `192.64.119.180`, `http://medio-punto.com` still redirects to `http://www.medio-punto.com/`, apex HTTPS still times out, and `https://www.medio-punto.com` still returns `405` with `server: Parking/1.0`.
- The main remaining Medio Punto product gap is still operational, not build health: the `Assisted order` block does not expose a real WhatsApp, DM, or payment path yet, so the storefront is honest but not fully ready for assisted sales.
- Revalidated `nctto.local/projects` on the same day. The local preview on `3104` had also fallen over and was causing `502` through ingress. After restarting `next start`, `/projects` and `/resources/landing-checklist` returned `200` again and the served content matched the current source in `github/bshop341-b/lumenforge`.
- Revalidated `mytzuko.local` and `stitching-octopus.local` after the same kind of preview regression. Fresh `npm run build` completed successfully for `github/nctto/mytzuko` and `github/rfcku/stitchingoctopus`, `PORT=3102 npm run start` and `PORT=3103 npm run start` are listening again, `http://127.0.0.1:3102/` still exposes the real WhatsApp + Instagram CTA (`wa.me`, `mytzuko.a.nidome`), `http://127.0.0.1:3103/` still exposes the current Stitching Octopus storefront signals (`Shop`, `Policies`, `stitchingoctopus`), and both `.local` ingress routes returned `HTTP/2 200` again when resolved to `172.18.255.200`.
- Pattern worth remembering: local `.local` ingress routes for these Next.js previews (`medio-punto`, `mytzuko`, `stitching-octopus`, `nctto`) are healthy when the backing `next start` process is alive, but they can silently fall over when the local preview process stops. The cluster wiring was not the root cause in these checks.
- `github/bshop341-b/kanban-dashboard-backend` is not currently blocked by app code. Removing the `# syntax=docker/dockerfile:1` directive from its Dockerfile and switching to `npm ci --omit=dev` reduced Dockerfile-specific friction, but local `docker build` still hangs at base-image metadata resolution for `node:20-bookworm-slim`, and even `DOCKER_BUILDKIT=0 docker build` still dies in this runtime. That points to the Docker runtime on this host more than the repo. Clean up orphaned `docker` or `docker-buildx` processes after these SIGKILLs.
- Audited `github/rfcku/ditto-cli` TUI end-to-end against real backend data, using a local proxy from `http://localhost:9001/api/v1` to `https://api.ditto.local/v1` because the client default base URL is hardcoded for local dev. The TUI is usable for onboarding/register/login, feed rendering, post detail, voting, comments, communities, command palette, and help.
- High-value `ditto-cli` audit findings: the UI/help advertises shortcuts like `n` (new post), `r` (refresh), and `s` (search/share) that are not consistently wired in runtime, share copies an API/proxy URL instead of a public post URL, `get posts --random` and `get communities --random` fail because the client expects `{data:[...]}` while the backend returns a raw array, `GetConfigPath()` ignores `XDG_CONFIG_HOME`, and session-expired handling is inconsistent, sometimes surfacing auth errors without clearing persisted token state cleanly.
- Earlier GitHub review decisions from the same workstream: `rfcku/ditto-cli#115` and `#118` were closed after confirming real implementation risks, while `#120` remained the only plausibly salvageable open PR.
- `github/rfcku/medio-punto` in the OpenClaw workspace is the current canonical copy and its `main` is confirmed at `1fbef60` on GitHub after the storefront push. Two other clean clones still exist at `/Users/rfcku/Sites/github/medio-punto` and `/Users/rfcku/.openclaw/github/rfcku/medio-punto`, both stale at `eef125b`, and should not be treated as source of truth.
- Access to the private repo `rfcku/blockchain` is available now. It is currently a tiny Go blockchain prototype/tutorial on `main` with just one visible commit (`c820c32`, `checkpoint`), no open issues or PRs, no CI, and no README.
- `rfcku/blockchain` currently builds, but it is very early-stage and not product-ready: no meaningful tests, committed artifacts like `blockchain` and `wallet.dat`, hardcoded Badger path `/tmp/badger`, and `addblock` hardcodes the coinbase receiver to `rfcku`.
- For future Ditto planning, the recommended blockchain direction is hybrid, not fully on-chain: keep posts/comments/votes/karma/daily internal rewards off-chain, use chain only for deposits, withdrawals, real tips, community treasury, and audit checkpoints. First build a transactional internal ledger, then add a backend blockchain adapter, likely starting with one asset like USDC on Base or Polygon.
- The future blockchain-integration idea for Ditto was saved to Obsidian at `/Users/rfcku/Documents/BishopVault/Projects/Ditto/Blockchain para Ditto.md` so it can be revisited later.
- Continued `github/rfcku/ditto-cli` cleanup after the earlier audit and landed the next open issue batch directly on GitHub. PR `#144` closed four UI issues together: `#126` post detail now has regression coverage for body rendering, `#127` now has a regression test proving feed previews clamp at 200 characters, `#128` was confirmed already working in `main` and got regression coverage for community description rendering, and `#129` got the real fix so `p` can open the selected author's profile from feed/post detail with fallback to `Author.ID` when `AuthorID` is empty.
- Added the internal debug logging feature requested by `rfcku/ditto-cli#65` and merged it as PR `#145`. The TUI now keeps an in-memory log buffer and exposes a `:logs` debug/manual-style view instead of relying only on external log files.
- After merging `#144` and `#145`, local `github/rfcku/ditto-cli` `main` was clean and updated at commit `53f01b0`.
- The remaining open `ditto-cli` issues after that pass were only `#67` (ASCII/ANSI image previews), `#69` (background notification polling and badge), and `#70` (local response caching). `#69` currently looks backend-blocked because the Ditto server code does not expose real notification or mention endpoints yet, so `#70` is the next most practical manual target before image previews.
- Finished the next `github/rfcku/ditto-cli` merge wave and landed PRs `#152` (README/docs sync), `#154` (cache scoped by instance/auth context), and `#153` (use personalized `/feed` for the TUI home view). `#153` conflicted after `#154`, had to be rebased onto updated `main`, then merged cleanly.
- After that merge wave, `github/rfcku/ditto-cli` was re-synced to clean `main` and got another source-code plus runtime audit, including real CLI calls against the Ditto API and another live TUI pass through the local proxy at `http://127.0.0.1:9001/api/v1` -> `https://api.ditto.local/v1`.
- Latest confirmed working `ditto-cli` paths in real runtime: CLI `register`, `login`, `get me`, `get feed`, random `get` endpoints, `create community`, `create post` for an existing community, and `describe post`; TUI feed, open post, vote, view community, view author profile, comment/reply, and share all worked.
- New `ditto-cli` bugs found and filed from that second real audit: `#156` CLI `vote post` uses the wrong target type, `#157` `create post` only resolves communities from the first page of community results, `#158` CLI error handling drops backend error details and often collapses them to generic status messages, and `#159` leave-community behavior is wired to the wrong route or method contract.
- New backend bug filed in `github/rfcku/ditto` as `#43`: media upload writes into `./.tmp` and fails on read-only deployments (`mkdir ./.tmp: read-only file system`). This was reproduced from real `ditto-cli` upload attempts, so the blocker is server-side, not a CLI-only bug.
- Current remembered `ditto-cli` tracker state after the second audit: no open PRs, and the open issues are `#151`, `#156`, `#157`, `#158`, and `#159`.
