# Fix issue-284 validation

This feature ensures that the provided URL argument is validated against proper URI formatting before being written to config.toml, preventing runtime errors and ensuring data integrity.

Fixes #284.