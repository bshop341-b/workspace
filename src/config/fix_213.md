# Fix for Issue #213: Completion command initialization guard

// In cmd/root.go or similar central package:
func shouldSkipRuntimeInit(cmd *cobra.Command) bool {
    // Check if the command is version OR completion
    return cmd == nil || cmd.Name() == versionCmd.Name() || cmd.Name() == "completion"
}

// The error handling logic for loading config should also be adjusted to gracefully skip the
// completion command if the config fails to load, treating it as an auxiliary tool.
